#include "lists.h"

int check_cycle(listint_t *list)
{
	listint_t *front, *back, *front;

	if (list == NULL)
		return (0);

	back = list;
	front = list;

	while (front->next != NULL)
	{

		return (0);
	}
}
